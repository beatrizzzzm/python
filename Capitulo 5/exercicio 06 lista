def area(base, altura):
    print(f'A area do trianguloe é: {(base * altura) / 2}')


base = int(input("Insira a base do triângulo: "))
altura = int(input("Insira a altura do triângulo: "))
area(base, altura)
